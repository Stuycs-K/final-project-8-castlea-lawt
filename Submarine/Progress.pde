public class Progress{
  ArrayList<Coordinate>tasks = new ArrayList<Coordinate>(10)//randomized or not? + placeholder c
  int taskCounter = 0;
  PImage[]images = new PImage[10] //placeholder, will manually add images later on
  public PImage camera(Coordinate loco){
   for(int i = 0; i < tasks.size(); i++){
     if(loco){
     
   }
   else{
    //make text thing for error msg at bottom of screen that shows up for a few seconds 
   }
   }
   
  }
}
